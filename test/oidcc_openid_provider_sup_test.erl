-module(oidcc_openid_provider_sup_test).
-include_lib("eunit/include/eunit.hrl").


get_list_test() ->
    % can't be tested using unittests
    % as the supervisor can't be mocked
    ok.

add_test() ->
    % can't be tested using unittests
    % as the supervisor can't be mocked
    ok.
