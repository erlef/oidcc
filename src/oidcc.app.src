{application, 
 oidcc, [
         {description, "a easy to use OpenID Connect client library"},
         {vsn, "rolling"},
         {id, "git"},
         {modules, []},
         {registered, [oidcc_sup]},
         {applications, [
                         kernel,
                         stdlib,
                         crypto,
                         ejwt,
                         base64url
                        ]},
         {mod, {oidcc_app, []}}
        ]}.
