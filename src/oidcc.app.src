{application, 
 oidcc, [
         {description, "an easy to use OpenID Connect client library"},
         {vsn, "rolling"},
         {id, "git"},
         {modules, []},
         {registered, [oidcc_sup]},
         {applications, [
                         kernel,
                         stdlib,
                         crypto,
                         ejwt,
                         uri,
                         gun
                        ]},
         {mod, {oidcc_app, []}}
        ]}.
